---
  - pip: name=docker-py
  - name: consul server
    docker:
      name: consul
      image: progrium/consul
      state: started
      ports:
        - 8300:8300
        - 8302:8302
        - 8400:8400
        - 8500:8500
        - 8600:8600
      volumes:
        - /data
      command: "-server -node {{ ansible_hostname }} -advertise {{ ansible_default_ipv4.address }} -advertise-wan 52.201.225.154 -dc "falconwiz-aws-us-east"-bootstrap -ui-dir /ui"
